-- ============================================================================
-- R55-D · 2026-07-11 · 数字员工板块整改 · 删所有测试类数字员工实例
-- ----------------------------------------------------------------------------
-- 背景:
--   R51-R54 阶段遗留一批测试类 / 实验类数字员工实例(R54testN 系列、
--   "测试Bot--验证缝合" 等),混在生产数据里,会污染矩阵列表的视觉与统计。
--
-- 目标:
--   1. 删除 name / display_name 命中模式 (test|R42X|R52-test|e2e|测试|fixture|backfill)
--      的数字员工实例,不动 HR 模板 (agent_templates 不在本表内,自动安全)。
--   2. 留下 1 个通用员工 (`不盈--全栈开发`),为后续 5.1 (新通用员工模板派生) 做准备。
--   3. bot_configs / runs / logs 等子表用 ON DELETE CASCADE / SET NULL 处理。
--
-- 幂等:三次执行不报错、不重复删除。
-- 回滚:本批删除的目标行已审计;若需恢复,从 db_backup_2026_07_08_q1.sql 抽回。
-- ============================================================================

BEGIN;

DELETE FROM agent_instances
WHERE name ~* 'test|R42X|R52-test|e2e|测试|fixture|backfill'
   OR display_name ~* 'test|R42X|R52-test|e2e|测试|fixture|backfill';

COMMIT;
