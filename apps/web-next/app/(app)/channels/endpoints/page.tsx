import { PagePlaceholder } from "@/components/layout/page-placeholder";

export default function Page() {
  return (
    <PagePlaceholder
      title="接入点"
      description="Webhook · 回调 · 公共入口"
      module="资源频道 (Channels)"
      route="/channels/endpoints"
    />
  );
}
